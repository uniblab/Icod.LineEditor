name: pr-build-and-test

on:
  pull_request:

permissions:
  contents: read

jobs:
  build-and-test:
    strategy:
      matrix:
        os: [windows-latest, ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x
      - run: dotnet clean Icod.LineEditor.sln -c Staging
      - run: dotnet restore Icod.LineEditor.sln
      - run: dotnet build Icod.LineEditor.sln -c Staging --no-restore
      - run: dotnet test Icod.LineEditor.sln -c Staging --no-build --logger trx
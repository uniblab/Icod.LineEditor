# Icod.LineEditor.Red.Tests

Command-level tests for the permanently restricted `red` executable.

The project verifies public identity, ordinary editing, equivalence with `ed --restricted`, direct and nested shell denial, host-independent pathname rejection, compatibility handling of `-r`, and permitted simple-name file operations beneath the captured working directory.
